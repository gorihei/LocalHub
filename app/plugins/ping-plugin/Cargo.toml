[package]
name = "ping-plugin"
version = "0.1.0"
edition = "2021"
description = "TCP接続確認によるホスト疎通チェックプラグイン"

[[bin]]
name = "ping-plugin"
path = "src/main.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
