[package]
name = "folder-size-plugin"
version = "0.1.0"
edition = "2021"
description = "指定フォルダの容量・サブフォルダ別サイズを集計するプラグイン"

[[bin]]
name = "folder-size-plugin"
path = "src/main.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
