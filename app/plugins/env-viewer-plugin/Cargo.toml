[package]
name = "env-viewer-plugin"
version = "0.1.0"
edition = "2021"
description = "環境変数・PATHを一覧表示するプラグイン"

[[bin]]
name = "env-viewer-plugin"
path = "src/main.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
